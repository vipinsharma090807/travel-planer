import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["restcountries.com", "flagcdn.com" ,'upload.wikimedia.org', 'openweathermap.org'],
  },
};

export default nextConfig;
